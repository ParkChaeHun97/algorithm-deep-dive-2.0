### 문제
- M 이상 N 이하의 소수의 합과 최솟값 출력
- 소수가 없으면 -1 출력

### 해결
- 에라토스테네스의 체로 N 이하 소수 판별
- M~N 범위 순회하며 합산 및 최솟값 갱신
- O(N log log N)으로 해결

### 핵심 포인트
- 소수 없는 케이스 처리: `min == Integer.MAX_VALUE`이면 -1 출력
- Queue로 최솟값 뽑으려 했으나 오름차순 순회라 min 변수 하나로 충분
- 매직넘버(1001) 대신 Integer.MAX_VALUE 쓰는 게 범용적