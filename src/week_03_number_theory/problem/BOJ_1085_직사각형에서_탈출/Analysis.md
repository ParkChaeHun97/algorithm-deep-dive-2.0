## 백준 1085 (직사각형에서 탈출)

### 문제
- (x, y) 위치에서 (0,0)~(w,h) 직사각형 경계까지의 최단 거리 출력

### 해결
- 네 방향 거리 계산: x, y, w-x, h-y
- Math.min으로 최솟값 선택
- O(1)로 해결

### 핵심 포인트
- 별다른 알고리즘 없이 네 방향 거리만 계산하면 끝
- 구현 문제는 조건을 그대로 코드로 옮기는 게 핵심
